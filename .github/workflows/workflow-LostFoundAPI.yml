name: Deploy LostFound Web API Lambda to AWS

# 언제 실행할지 정의: main 브랜치에 push될 때
on:
  push:
    branches:
      - main
    paths:
      - 'LostFoundAPI/**'

  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'
        required: true
        default: 'warning'
        type: choice
        options:
          - info
          - warning
          - debug

jobs:
  deploy:
    # Docker를 사용하므로 ubuntu-latest를 유지
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      # 1. GitHub 저장소의 코드 이용
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Python 환경 설정 단계는 제거 (Docker 사용)

      # 3. 라이브러리를 설치하고 배포용 zip 생성 (Lambda 호환 빌드)
      - name: Install dependencies and create ZIP file (Lambda-compatible build)
        run: |
          # 1. LostFoundAPI 디렉토리로 이동
          cd LostFoundAPI
          
          docker run --rm \
            --entrypoint "/bin/bash" \
            -v "$PWD":/var/task \
            public.ecr.aws/lambda/python:3.12-x86_64 -c "pip install -r /var/task/requirements.txt -t /var/task"

          sudo find . -type d -name "__pycache__" -exec rm -rf {} +
          
          zip -r ../LostFoundAPI.zip . -x "tests/*"

      # 4. AWS 자격 증명을 설정 및 배포
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::327784329358:role/inha-capstone-14-cicd-role
          aws-region: us-west-2

      - name: Deploy to AWS Lambda
        run: |
          aws lambda update-function-code \
            --function-name LostFoundAPI \
            --zip-file fileb://LostFoundAPI.zip
